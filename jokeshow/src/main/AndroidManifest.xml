<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.teachableapps.gradlebuilditbigger.jokeshow" >

    <application
        android:allowBackup="false"
        android:label="@string/app_name">
        <activity
            android:name=".JokeActivity"
            android:label="@string/app_name" />
    </application>

</manifest>